apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: registry-ghcr
  namespace: argocd
spec:
  source:
    targetRevision: stage
    helm:
      parameters:
        - name: "httpproxy.fqdn"
          value: ghcr.registry.stage0.cybozu-ne.co
