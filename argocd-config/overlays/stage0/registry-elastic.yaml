apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: registry-elastic
  namespace: argocd
spec:
  source:
    targetRevision: stage
    helm:
      parameters:
        - name: "httpproxy.fqdn"
          value: elastic.registry.stage0.cybozu-ne.co
