apiVersion: v1
kind: Service
metadata:
  name: elastic-registry
  namespace: registry
spec:
  selector:
    app.kubernetes.io/name: elastic-registry
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
