apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: quay-registry
  namespace: registry
spec:
  virtualhost:
    fqdn: quay.registry.gcp0.dev-ne.co
  routes:
    - conditions:
        - prefix: /
      services:
        - name: quay-registry
          port: 5000
