apiVersion: v1
kind: Service
metadata:
  name: quay-registry
  namespace: registry
spec:
  selector:
    app.kubernetes.io/name: quay-registry
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
