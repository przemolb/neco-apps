apiVersion: apps/v1
kind: Deployment
metadata:
  name: quay-registry
  namespace: registry
spec:
  template:
    spec:
      containers:
        - name: registry
          envFrom:
            - secretRef:
                name: registry-bucket
            - configMapRef:
                name: registry-bucket
            - secretRef:
                name: quay-credential-t5c62bctdb
